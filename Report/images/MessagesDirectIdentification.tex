\begin{lstlisting}[language=Java]
/*
 * R ed S sono rispettivamente gli Identificatori del Processo "Sender" e del Processo "Receiver".
 * La Variabile "message" sta ad indicare il messaggio inviato dal Processo "Sender" e che il Processo "Receiver" dovra' ricevere.
 */
send(R, message);
receive(S, message);
\end{lstlisting}