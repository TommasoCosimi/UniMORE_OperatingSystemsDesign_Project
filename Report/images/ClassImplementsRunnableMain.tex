\begin{lstlisting}[language=Java, numbers=left, stepnumber=1]
...
for(int i=0; i<NUM_OF_THREADS; i++) {
    // Creo un oggetto di tipo ImplementRunnableRunnable che estenda Runnable
    ImplementRunnableRunnable irr = new ImplementRunnableRunnable(i);
    // Creo un Thread a partire da esso
    Thread t = new Thread(irr);
    // Avvio il Thread
    t.start();
}
...
\end{lstlisting}
\captionof{lstlisting}{Classe Main per l'utilizzo della Classe precedentemente illustrata.}